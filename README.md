# ical-proxy

### Fetch ics file

http\<s\>://\<ip-address\>:\<port\>/ical_url?url=\<your_url_to_the_ics_file\>

You have to build your own Dockerfile because I am to lazy to create an Docker account.

Just execute this command: docker build -t ical-proxy .